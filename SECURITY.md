# Security Policy

This project is a static client and local data pipeline. It should not contain production credentials or private source data.

Report a sensitive vulnerability through the repository's private security-advisory channel when available. Do not post API keys, tokens, private datasets, or exploitable details in a public issue.

For non-sensitive bugs, use the public issue tracker and include reproduction steps, the affected election/mode, and the relevant build or commit identifier.
